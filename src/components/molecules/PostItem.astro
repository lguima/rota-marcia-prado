---
import { type CollectionEntry } from 'astro:content'
import { formatDate } from '@utils/date'

export interface Props {
  post: CollectionEntry<'article'>
}

const { post } = Astro.props
const { title, publishDate, category } = post.data

const formattedPublishDate = formatDate(publishDate)
---

<a href={`/artigos/${post.id}/`} class="item flex flex-col sm:flex-row items-start">
  <section>
    <h3 class="lg:text-xl">
      {title}
    </h3>
    <p>
      <span class="badge" data-variant="secondary">
        {category}
      </span>
    </p>
  </section>
  <section>
    <p>{formattedPublishDate}</p>
  </section>
</a>
